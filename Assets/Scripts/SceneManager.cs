using UnityEngine;
using System.Collections;

public class SceneManager : MonoBehaviour
{
    //Stores the difficulty into the Player Prefs throughout the screens
    int difficulty;

	void Update ()
    {
        ////Here goes the logic for the button clicking. Upon clicking the button, an int must be set using the playerPrefs to set up the game.
        ///*if (Button1.Click)
        // *{*/
        //PlayerPrefs.SetInt("Difficulty", 0);
        //return;
        ///*}*/

        ///*else if (Button2.Click)
        // *{*/
        //PlayerPrefs.SetInt("Difficulty", 1);
        //return;
        ///*}*/

        ///*else if (Button2.Click)
        // *{*/
        //PlayerPrefs.SetInt("Difficulty", 2);
        //return;
        ///*}*/
    }
}
